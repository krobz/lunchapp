package com.example.lunchapp.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/**
 * A configuration class for web-related settings and configurations.
 *
 * This class is annotated with @Configuration, indicating that it is a configuration class
 * that provides bean definitions and bean dependencies for the application context.
 */
@Configuration
public class WebConfig {

}

